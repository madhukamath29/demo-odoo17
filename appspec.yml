version: 0.0
os: linux
files:
  - source: /temp/test/  # Referencing the directory created in CodeBuild
    destination: opt/test1  # Example destination on your EC2 instance

hooks:
  AfterInstall:
    - location: scripts/after_install.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: scripts/start_server.sh
      timeout: 300
      runas: root
